package com.ekekonomia.client.commands;

// Client commands disabled to prevent StackOverflowError
// Use server-side /tienda command instead

public class ClientCommands {
    
    public static void register() {
        System.out.println("[Ekekonomia] Client commands disabled to prevent StackOverflowError");
        System.out.println("[Ekekonomia] Use server-side /tienda command instead");
    }
}
